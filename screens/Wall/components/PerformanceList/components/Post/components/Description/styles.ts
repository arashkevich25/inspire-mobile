import { StyleSheet } from 'react-native';

import { winWidth } from 'tools';

export const Styles = StyleSheet.create({
    contentContainer: {
        width: winWidth - 40,
    },
    text: { lineHeight: 22 },
});
