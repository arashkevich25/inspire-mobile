import EStyleSheet from 'react-native-extended-stylesheet';

export const Styles = EStyleSheet.create({
    imageContainer: { width: 30, height: 30 },
    bodyContainer: { borderBottomWidth: 0, paddingRight: 20 },
});
