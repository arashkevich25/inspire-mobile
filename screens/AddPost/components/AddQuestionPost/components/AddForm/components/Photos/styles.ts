import { StyleSheet } from 'react-native';

export const Styles = StyleSheet.create({
    contentContainer: {
        marginRight: 20,
        marginLeft: 20,
    },
    photoChooseContainer: {
        marginTop: 10,
        alignItems: 'center',
    },
    text: {
        marginBottom: 10,
    },
});
