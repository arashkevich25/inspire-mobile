import EStyleSheet from 'react-native-extended-stylesheet';

export const Styles = EStyleSheet.create({
    contentContainer: { width: 60, alignItems: 'center', position: 'relative' },
    badgeBox: { position: 'absolute', top: 15, zIndex: 1, right: 20 },
});
