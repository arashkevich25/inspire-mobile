import EStyleSheet from 'react-native-extended-stylesheet';

export const Styles = EStyleSheet.create({
    recommendsCount: { marginBottom: 30, marginTop: 10 },
    recommendsCountText: { textAlign: 'center' },
    recommendsCountAdditionalText: { textAlign: 'center' },
});
