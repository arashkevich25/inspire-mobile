import EStyleSheet from 'react-native-extended-stylesheet';

export const Styles = EStyleSheet.create({
    contentContainer: {
        height: 120,
        width: 120,
        alignItems: 'center',
        justifyContent: 'center',
    },
    nameBox: { position: 'absolute', bottom: 0, left: 0, right: 0 },
});
